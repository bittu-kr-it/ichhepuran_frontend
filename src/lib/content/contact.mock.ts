import type { ContactHeroContent } from "../types";

// Drafted copy, not migrated — see ContactPageSeeder.php's docblock.
export const contactHeroMock: ContactHeroContent = {
  headline: "We'd Love to Hear From You",
  subheading: "Questions, ideas, or just want to say hello — reach out and our team will get back to you.",
};
